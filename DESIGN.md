---
name: Blueprint AI Training
description: Consulting-grade calm in the BCG mold, with Malaysia in the gradients.
colors:
  blue-deep: "#0b2161"
  blue-deeper: "#06103a"
  blue: "#1e40c9"
  blue-mid: "#4f7df0"
  blue-soft: "#7fa4ff"
  blue-royal: "#1230a0"
  blue-indigo: "#10298c"
  glint-red: "rgba(226, 59, 59, 0.4)"
  glint-amber: "rgba(255, 179, 92, 0.45)"
  glint-red-soft: "rgba(226, 59, 59, 0.22)"
  glint-amber-soft: "rgba(255, 179, 92, 0.35)"
  pill: "#bcd2ff"
  pill-hover: "#a7c3ff"
  panel-light: "#e4ecff"
  white: "#ffffff"
  paper: "#f4f4f2"
  silver: "#d8d8d6"
  slate: "#4d4d4d"
  ink: "#1a1a1a"
typography:
  display:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "clamp(2.125rem, 3.8vw, 3.125rem)"
    fontWeight: 500
    lineHeight: 1.14
    letterSpacing: "-0.01em"
  headline:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "clamp(1.625rem, 2.6vw, 2.25rem)"
    fontWeight: 500
    lineHeight: 1.18
  title-serif:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "clamp(1.375rem, 2vw, 1.75rem)"
    fontWeight: 500
    lineHeight: 1.22
  subhead:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "1.625rem"
    fontWeight: 500
    lineHeight: 1.22
  quote:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "clamp(1.375rem, 2.2vw, 1.75rem)"
    fontWeight: 400
    lineHeight: 1.42
  mosaic-title:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "clamp(1.375rem, 2.2vw, 1.875rem)"
    fontWeight: 500
    lineHeight: 1.2
  profile-name:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "1.5rem"
    fontWeight: 500
  float-title:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "1.375rem"
    fontWeight: 500
    lineHeight: 1.25
  initials:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "3rem"
    fontWeight: 500
  body:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "1.0625rem"
    fontWeight: 400
    lineHeight: 1.65
  lede:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "clamp(1.0625rem, 1.4vw, 1.1875rem)"
    fontWeight: 400
    lineHeight: 1.6
  title:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "1.1875rem"
    fontWeight: 600
    lineHeight: 1.3
  brand:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "1.125rem"
    fontWeight: 700
  action:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "0.8125rem"
    fontWeight: 600
    letterSpacing: "0.06em"
  nav:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "0.9375rem"
    fontWeight: 500
  small:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "0.875rem"
    fontWeight: 400
  meta:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "0.8125rem"
    fontWeight: 400
  label:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "0.75rem"
    fontWeight: 600
    letterSpacing: "0.1em"
  chip:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "0.75rem"
    fontWeight: 600
    letterSpacing: "0.08em"
  micro:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "0.6875rem"
    fontWeight: 600
    letterSpacing: "0.08em"
  day-title:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "1.625rem"
    fontWeight: 500
  strength-num:
    fontFamily: "Source Serif 4, Georgia, serif"
    fontSize: "clamp(3rem, 4.6vw, 4.25rem)"
    fontWeight: 500
    lineHeight: 0.9
    letterSpacing: "-0.02em"
  session-title:
    fontFamily: "Archivo, Helvetica Neue, Arial, sans-serif"
    fontSize: "1.0625rem"
    fontWeight: 600
rounded:
  card: "16px"
  panel: "24px"
  pill: "999px"
  mark: "3px"
spacing:
  section-y: "clamp(2rem, 4vw, 3rem)"
  pad-x: "clamp(1.25rem, 4vw, 3rem)"
  card-gap: "1.25rem"
components:
  button-pill:
    backgroundColor: "{colors.pill}"
    textColor: "{colors.blue-deep}"
    rounded: "{rounded.pill}"
    padding: "0.85rem 1.6rem"
  button-pill-hover:
    backgroundColor: "{colors.pill-hover}"
    textColor: "{colors.blue-deep}"
  button-pill-dark:
    backgroundColor: "{colors.blue-deep}"
    textColor: "{colors.white}"
    rounded: "{rounded.pill}"
    padding: "0.85rem 1.6rem"
  button-outline:
    backgroundColor: "transparent"
    textColor: "{colors.ink}"
    rounded: "{rounded.pill}"
    padding: "0.85rem 1.6rem"
  card:
    backgroundColor: "{colors.white}"
    rounded: "{rounded.card}"
    padding: "1.4rem"
  panel-deep:
    backgroundColor: "{colors.blue-deep}"
    textColor: "{colors.white}"
    rounded: "{rounded.panel}"
  panel-light:
    backgroundColor: "{colors.panel-light}"
    rounded: "{rounded.panel}"
---

# Design System: Blueprint AI Training

## Overview

**Creative North Star: "Blueprint Blue"**

Blueprint AI Training carries itself the way today's bcg.com does, and the user pinned that reference with screenshots: a calm white ground on which everything interesting happens inside inset, rounded panels. Nothing floods edge to edge. Serif display type (Source Serif 4) gives statements their authority; Archivo does the working text; actions are uppercase pill buttons with a sliding arrow. The visual temperature is set by a blue family anchored on a deep royal blue, with a light-blue pill as the signature action color.

Malaysia is present but never literal. The national colors (blue leads, with red and amber) live inside the gradient art that fills hero cards, card covers and placeholder media: soft radial glints of red and amber breathing inside deep blue fields. No flags, no hibiscus, no patriotic iconography. The one photograph in the system, Kuala Lumpur at dusk, carries the sense of place.

**Key Characteristics:**
- White page ground; all color arrives inside rounded inset panels and cards
- Serif display over sans working text, the consulting-firm pairing
- Pill buttons, uppercase with arrows; chips for facts and filters
- Gradient art carries the Malaysian palette as glints, never as flat national color
- The insights mosaic: one tall photo card, one gradient card, one dark view-all card
- Footer in two layers: light paper columns over a deep blue bar

## Colors

Normative values live in the frontmatter.

### Primary
- **Deep Blue** (`blue-deep`): the dark panel ground and the text color on pill buttons. **Deeper Blue** (`blue-deeper`) is the footer bar and the view-all mosaic card.
- **Accent Blue** (`blue`): links, active states, small accents, the brand mark. AA-safe on white.
- **Pill Blue** (`pill`, hover `pill-hover`): the signature action ground; deep blue text on it.

### Secondary
- **Panel Light** (`panel-light`): the light blue panel ground for assist and claiming sections.
- **Blue Mid / Blue Soft** (`blue-mid`, `blue-soft`): gradient art only, never UI chrome.

### Malaysian glints
- Red `rgba(226,59,59,…)` and amber `rgba(255,179,92,…)` appear only inside gradient art (`.art`, `.art--soft`, `.cover-a/b/c`). They are atmosphere, not identity blocks.

### Neutral
- **White**: the page ground. **Paper** (`paper`): light gray panels and the upper footer. **Silver** (`silver`): 1px hairlines. **Slate** (`slate`): secondary text. **Ink** (`ink`): text.

### Named Rules
**The Inset Rule.** Color never runs edge to edge. Every colored surface is a rounded panel or card floating inside the container; the only full-width surfaces are the header and the footer layers.

**The Glint Rule.** Malaysian red and amber exist only inside gradients, as radial glints in a blue field. They never appear as solid UI colors, borders, or text.

## Typography

**Display Font:** Source Serif 4 (Google Fonts; fallback Georgia)
**Working Font:** Archivo (fallback Helvetica Neue, Arial)

**Character:** The serif speaks in headlines, card titles on imagery, day headers and pull quotes; it is always weight 400 or 500, never bold. Archivo handles body, UI, labels, chips and buttons. Pill buttons are uppercase Archivo 600 with tracked letters and an arrow.

Roles are normative in the frontmatter. Eyebrow labels are uppercase tracked, blue on light grounds, translucent white on dark. Not every section gets an eyebrow.

### Named Rules
**The Quiet Serif Rule.** The serif never goes bold and never shouts; authority comes from size and space, not weight.

## Layout

White ground, one column of inset panels and grids inside a 1280px container. The canonical page shape: centered serif intro (eyebrow, display, short lede), then a full-container rounded hero card (gradient art with a floating white card on home), then alternating grids and panels. Cards sit in 3- or 4-column grids (collapsing 2 then 1); splits are two-column panel interiors collapsing at 860px. Section rhythm is `section-y` (deliberately tight, per the founder's request; blocks sit close and the panels' own internal padding provides the air).

The insights mosaic is a 2-column grid where the lead article spans two rows as a photo card; the second article is a gradient card; the third cell is the dark "view all articles" card.

## Elevation & Depth

Flat surfaces with soft, deep shadows reserved for floating elements: the hero's white floating card (`0 18px 44px rgba(11,33,97,0.22)`), card hover (`0 14px 34px rgba(26,26,26,0.12)` with a 3px lift), and the sticky header once scrolled. Photo cards darken toward their caption edge with a deep-blue gradient scrim. The header is translucent white with an 8px backdrop blur.

### Named Rules
**The Float Rule.** A shadow means the element floats above a panel (hero card, hover, sticky nav). Nothing at rest inside the page column carries one.

## Shapes

Rounded and soft: 24px panels, 16px cards, full pills for buttons and chips, 3px radius on the small brand mark. Hairlines are 1px silver. Imagery is always inside a rounded container, never full-bleed. No sharp corners anywhere in the v2 world.

## Components

### Buttons
- **Pill** (`btn--pill`): pill-blue ground, deep-blue text, uppercase 0.8125rem, arrow slides 4px on hover. The dark variant inverts (deep blue ground, white text) for light panels.
- **Outline** (`btn--outline`, `btn--outline-light`): 1px border pill, fills on hover.

### Chips
Pill chips in a hairline-bordered pill tray (`chip-row`); the active chip is deep blue with white text. Card tags are small white or ghost pills sitting on covers.

### Cards
White ground, 1px silver border, 16px radius, 16:10 cover (photo or gradient `cover-a/b/c`), body with sans title and meta. Hover lifts 3px with the ambient shadow.

### Panels
`panel--deep` (deep blue, white text), `panel--light` (light blue), `panel--paper` (light gray), all 24px radius with `panel-split` two-column interiors. The deep panel is the emphasis moment; light panels carry conversion and claiming.

### Mosaic cards
Photo or gradient ground, bottom-anchored: eyebrow, serif title, pill button. A deep-blue scrim rises from the bottom on photo cards. The dark variant (view-all) is flat `blue-deeper`.

### Strengths band (home)
One white 24px panel divided into four hairline columns: a 30px stroke icon in accent blue (inline Lucide-style SVG, stroke-width 2, round caps), a sans title, plain copy, then a large serif numeral pinned to the bottom beside a small slate label. Numerals (`strength-num`) are Source Serif 4 weight 500 with a blue gradient fill running blue-deep to blue-mid; the gradient stays inside the blue family, never red or amber, so the Glint Rule holds. Only truthful numbers appear here (2 engineers, 1st-hand, 1 use case each, 0 coding); never invent a stat to fill a cell.

### Workshop tree (home)
A two-column gradient art panel: on the left, the eyebrow, headline, a compact floating white root card for the flagship (its shadow is earned under the Float Rule), the view-all pill and a small caveat note; on the right, a deliberately untidy constellation of department pills at scattered positions, joined to the left edge by thin dashed SVG curves whose dash offset animates slowly (the section's one motion moment; it stops under reduced motion). The three in-development courses are solid white pills; other departments are translucent ghost pills; the dashed "Your department" pill links to the request card on workshops.html. Node pills are positioned by inline left/top percentages and the SVG curves are drawn to match. Below 1080px the map becomes a plain wrapped pill cloud with the curves hidden.

### Signature: gradient art
Layered radial gradients on a deep blue base with one amber and one red glint (`.art`; `.art--soft` for light panels; `cover-a/b/c` for cards). This is where Malaysia lives in the design. Never add flags, flowers, or literal national symbols.

## Do's and Don'ts

### Do:
- **Do** keep every colored surface inset and rounded (The Inset Rule); the white ground breathes between panels.
- **Do** set statements in Source Serif 4 at weight 400 to 500 and let size carry authority.
- **Do** use pill buttons with uppercase labels and arrows for every action.
- **Do** carry the Malaysian palette only as gradient glints inside blue fields (The Glint Rule).
- **Do** write copy like a person: plain sentences, no em dashes, no slogan cadence.

### Don't:
- **Don't** run color bands edge to edge; the v1 full-bleed grammar is retired.
- **Don't** use literal national imagery: no hibiscus, no flags, no patriotic motifs.
- **Don't** bold the serif or use it for body text.
- **Don't** use AI clichés: neon circuits, glowing brains, particle fields.
- **Don't** put red or amber on UI chrome; they exist only inside the art.
